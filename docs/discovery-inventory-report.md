# Discovery-Inventory Report — Phase Ω(B) (A)

**Phase:** Ω(B) — Explanation Boundary Discovery Engine ·
**Method version:** `explanation-boundary-1.0` · **Date:** 2026-06-08.
*(Named Ω(B) to avoid collision with the earlier Phase Ω "World Model" engine.)*

## 1. Objective
Inventory the project's stable discoveries (Phase-11 survivors and later results), each
with definition, strength, stability, and scope — the set of explanatory tools whose
combined power the rest of the phase measures.

## 2. Method
Curated synthesis from the project record (not re-derived): only findings that survived
robustness/falsification are admitted. Each is tagged with its established strength and the
deflation/limit later phases attached to it.

## 3. Results
Nine stable discoveries are inventoried (`discovery_inventory.json`), including: the
frequency hub (reduces to lexical frequency, Phase 16); internal consistency (generic, Phase
15/17); the motif vocabulary (3/13 survive the frequency null); **the proposition/co-occurrence
network (74.7% structure, 3.9× null — the strongest genuine non-frequency structure)**; the
SCC core; scale-invariance/homogeneity (Phases 13/14); the local grammar; the relational (not
referential) semantic layer (Phase Σ); and — binding everything — **D_nonpredictive: the
structure is NON-PREDICTIVE beyond frequency out-of-sample (Phase P)**.

## 4. Interpretation
The inventory's centre of gravity is two facts in tension: a genuine relational network
exists beyond frequency (Phase 17), yet it does not predict held-out content (Phase P). The
explanation-boundary measurement turns on reconciling them: real structure that does not
generalize.

## 5. Falsification Attempts
Inventory only; each item already carries its own falsification history. Items that failed
(e.g. foundational principles, Phase 8) are excluded.

## 6. Limitations
Strengths are cited from prior phases at their stated scope; the inventory does not re-run
them. The list is the admissible toolset, not a ranking.

## 7. Conclusion
Nine stable discoveries form the explanatory toolset; the binding constraint is Phase P's
non-predictiveness. These are combined and measured in the following reports.

Source: `generated/explanation_boundary/discovery_inventory.json`.
