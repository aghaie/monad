# Selection-Classification Report — Phase Φ (H taxonomy)

**Phase:** Φ · **Method version:** `counterfactual-discovery-1.0` · **Date:** 2026-06-08.

## 1. Objective
Classify the Phase Ψ residual from evidence: unconstrained freedom, weakly/strongly constrained
selection, near-unique selection, or unknown.

## 2. Method
Deterministic function of the measured residual fraction: >0.6 → weakly constrained; 0.3–0.6 → strongly
constrained; 0.05–0.3 → near-unique; <0.05 → unconstrained. (The residual is the lexical-identity
freedom; the structural form constraint is recorded separately.)

## 3. Results
Residual fraction = 0.796 (>0.6) → **TYPE_B — weakly constrained selection.**

## 4. Interpretation
The Phase Ψ residual classifies, from evidence, as **weakly constrained selection (TYPE_B)**: the
lexical-identity choice is reduced only ~20% by frequency and ~0 by structure, leaving ~80% free, and
the actual choices are typical within that freedom. The text's *coherent form* is strongly selected
(z ≈ 306), but the *residual* — the specific lexical identity — is weakly constrained. So the unexplained
80% is best described as weakly-constrained free selection, not strong or near-unique selection.

## 5. Falsification Attempts
TYPE_C/D (strongly constrained / near-unique) are falsified by the 79.6% residual; TYPE_A
(unconstrained) is falsified by the real 20% frequency constraint. Only TYPE_B survives.

## 6. Limitations
The taxonomy classifies the lexical-identity residual; the strong form-constraint (coherence) is a
separate, recorded axis.

## 7. Conclusion
**TYPE_B — weakly constrained selection.** The unexplained 80% is weakly-constrained free lexical choice
within a strongly-selected coherent form.

Source: `generated/counterfactual/selection_classification.json`.
