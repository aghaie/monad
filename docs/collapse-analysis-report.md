# Collapse-Analysis Report — Phase Ξ (H)

**Phase:** Ξ · **Method version:** `foundation-audit-1.0` · **Date:** 2026-06-08.

## 1. Objective
Classify discoveries that disappear when the representation changes.

## 2. Method
Four-way classification: TYPE_A artifact, TYPE_B weakly dependent, TYPE_C strongly dependent, TYPE_D robust.

## 3. Results
- **TYPE_A (artifact):** principles (0/16 survived, Phase 8), decision_architecture (91% frequency, Phase
  Δ), numerical_structure (no significance after correction, Phase 19X).
- **TYPE_B (weak):** consistency (robust but generic/tautological), motif_vocabulary (3/13 beat the null),
  reality_sunan (survives internally but vocabulary-based, internal-only).
- **TYPE_C (strongly dependent):** concept_partition, proposition_graph, compression, identities, grammar,
  relational_semantics, world_model, methodology_epistemology (all concept-clustering-dependent, Phase 11).
- **TYPE_D (robust):** the 6 information-theoretic invariants.

## 4. Interpretation
The collapse is concentrated where expected: the **conceptual edifice (TYPE_C, 8 discoveries)** strongly
depends on the concept-clustering representation (A3), and the **already-failed/deflated phases (TYPE_A, 3)**
were artifacts to begin with. The TYPE_B middle (consistency, motifs, reality-sunan) survive in a weak or
trivial sense. Only TYPE_D is durable. The dominant collapse mode is **representation-dependence via A3**,
exactly as the dependency map predicted.

## 5. Falsification Attempts
Each TYPE_A/C assignment rests on a prior control that already showed collapse (ARI, null, prediction); the
classification is evidence-driven, not assigned.

## 6. Limitations
Classification is per discovery; the evidence field records partial nuances (e.g. identity anchors are
moderate even where counts are method-relative).

## 7. Conclusion
**Collapse is concentrated in the conceptual edifice (TYPE_C, 8) and the failed phases (TYPE_A, 3);** only
the 6 information-theoretic TYPE_D findings are robust to representation change.

Source: `generated/foundation_audit/collapse_analysis.json`.
