# Assumption-Removal Report — Phase Ξ (C)

**Phase:** Ξ · **Method version:** `foundation-audit-1.0` · **Date:** 2026-06-08.

## 1. Objective
Remove each assumption and measure what collapses and what survives.

## 2. Method
For each assumption, apply its removal test and record the downstream collapse, using measurement (this
phase's representation rebuilds) and prior-phase results.

## 3. Results
- **Remove A1 (roots):** rebuild at lemma/word — the information-theoretic invariants HOLD (residual,
  coherence); vocabulary-based findings (methodology, reality, decision) shift.
- **Remove A2 (similarity captures structure):** concepts cannot form — the concept partition and
  everything downstream collapses; coherence-beyond-null is still measurable directly.
- **Remove A3 (clustering meaningful):** concepts/identities/principles/semantics/world-model/methodology
  become method-relative (Phase 11, ARI 0.22) — **STRONG collapse of the conceptual edifice.**
- **Remove A4 (edges real):** 65% is frequency (Phase 17); motifs 3/13 survive; grammar/compression/
  consistency-significance weaken.
- **Remove A5 (graph appropriate):** held-out prediction fails (Phase P) — the graph carries no
  generalizable information beyond frequency.

## 4. Interpretation
Removing **A3** is catastrophic for the conceptual edifice — the concept boundaries that everything is
built on are method-relative, so concepts/identities/principles/semantics/world-model/methodology lose
their footing. Removing **A1** is survivable: the information-theoretic core is recomputed at lemma/word
and holds. So the project's vulnerability is concentrated in A3 (and A2 that feeds it), not A1.

## 5. Falsification Attempts
This report is the removal experiment; the conceptual edifice collapses under A3, the core survives all.

## 6. Limitations
Removals use prior-phase verdicts + this phase's rebuilds rather than literally re-running Phase 3
clustering per representation (infeasible); the ARI evidence is decisive regardless.

## 7. Conclusion
**Removing A3 collapses the conceptual edifice; the information-theoretic core survives removal of all
five assumptions.**

Source: `generated/foundation_audit/assumption_removal_results.json`.
